export enum LocalStorageKeys {
    LoggedUserName = "LoggedUserName",
}

export enum Role {
    "ADULT" = 0,
    "CHILD" = 1
}

export enum RoutingConstants {
    HomePage = "home",
    Login = "login",
    Register = "register",
    Thermostat = "thermostat",
    LightingControl = "lighting-control",
    GroceriesDatabase = "groceries-database",
    ScreenTime = "screen-time",
    MedicineTimer = "medicine-timer",
    SecurityControl = "security-control",
    VacuumCleaner = "vacuum-cleaner",
    Music = "music",
    FlowerWatering = "flower-watering"
}

export enum RoutinConstatantsForAuthGuard {
    HomePage = "/home",
    Login = "/login",
    Register = "/register",
    Thermostat = "/thermostat",
    LightingControl = "/lighting-control",
    GroceriesDatabase = "/groceries-database",
    ScreenTime = "/screen-time",
    MedicineTimer = "/medicine-timer",
    SecurityControl = "/security-control",
    VacuumCleaner = "/vacuum-cleaner",
    Music = "/music",
    FlowerWatering = "/flower-watering"
}